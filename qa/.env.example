# ComfyGit QA Docker Configuration
# Copy to .env and edit for your setup
#
# All paths must be accessible to the Docker daemon.
# In Docker-in-Docker setups, use HOST paths (not container paths).

# === Claude Authentication ===
# Path to directory containing .credentials.json
# Direct host: ~/.claude
# DinD/ACFS: Use the shared auth volume path, e.g., /var/lib/docker/volumes/acfs-setup_claude-auth/_data
# Or just set ANTHROPIC_API_KEY instead
CLAUDE_AUTH_PATH=~/.claude

# Alternative: Use API key directly (no path needed)
# ANTHROPIC_API_KEY=sk-ant-...

# === ComfyGit Repository ===
# Path to comfygit repo root (for local development)
# Direct host: .. (parent of qa directory)
# DinD: Full host path, e.g., /home/youruser/dev/projects/comfygit-ai/comfygit
COMFYGIT_PATH=..

# === QA Paths (optional, defaults work for direct host) ===
# Only set these if you need to override for DinD
# QA_REPORTS_PATH=./reports
# QA_SCENARIOS_PATH=./scenarios
# QA_INSTRUCTIONS_PATH=./agent_instructions

# === Example: ACFS Docker-in-Docker Setup ===
# Find your host dev path: docker inspect acfs-dev | grep '"Source".*dev"'
# Uncomment and edit these:
# CLAUDE_AUTH_PATH=/var/lib/docker/volumes/acfs-setup_claude-auth/_data
# COMFYGIT_PATH=/home/YOURUSER/dev/projects/comfygit-ai/comfygit
# QA_REPORTS_PATH=/home/YOURUSER/dev/projects/comfygit-ai/comfygit/qa/reports
# QA_SCENARIOS_PATH=/home/YOURUSER/dev/projects/comfygit-ai/comfygit/qa/scenarios
# QA_INSTRUCTIONS_PATH=/home/YOURUSER/dev/projects/comfygit-ai/comfygit/qa/agent_instructions
